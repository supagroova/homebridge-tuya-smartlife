---
phase: 04-switches-outlets-mapping-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mappers/scaling.ts
  - src/mappers/scaling.test.ts
  - src/mappers/switchOutlet.ts
  - src/mappers/switchOutlet.test.ts
autonomous: true
requirements: [SW-02, SW-03]
must_haves:
  truths:
    - "Switch/outlet mapping is pure and has no Homebridge imports."
    - "kg and tdq categories map to HomeKit Switch semantics."
    - "cz and pc categories map to HomeKit Outlet semantics."
    - "Multi-gang devices produce one mapping per switch DP."
    - "Integer scaling primitive implements raw / 10**scale and reverse conversion."
  artifacts:
    - path: "src/mappers/scaling.ts"
      provides: "Reusable Tuya integer scaling primitive"
    - path: "src/mappers/switchOutlet.ts"
      provides: "Pure switch/outlet DP mapping"
decisions_covered: [SW-02, SW-03]
---

<objective>
Create the pure mapping foundation for Phase 4: Tuya integer scaling utilities and switch/outlet
DP discovery/mapping.
</objective>

<context>
@.planning/phases/04-switches-outlets-mapping-engine/04-CONTEXT.md
@.planning/phases/04-switches-outlets-mapping-engine/04-RESEARCH.md
@src/discovery/types.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing scaling primitive tests</name>
  <action>
    Create `src/mappers/scaling.test.ts`. Cover parsing Tuya integer `values` JSON, scaling raw
    integer values to HomeKit numbers, reverse conversion to raw integer values, and scaled
    min/max/step output.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/mappers/scaling.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 2: Implement scaling primitive</name>
  <action>
    Add `src/mappers/scaling.ts` with small pure helpers. Invalid/missing `values` should return
    `undefined` rather than throwing.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/mappers/scaling.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 3: Write failing switch/outlet mapper tests</name>
  <action>
    Create `src/mappers/switchOutlet.test.ts`. Cover single switch, outlet category mapping,
    multi-gang ordering, USB switch DP inclusion, ignoring non-switch DPs, and unsupported
    categories returning no mappings.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/mappers/switchOutlet.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 4: Implement switch/outlet mapper</name>
  <action>
    Add `src/mappers/switchOutlet.ts`. Return plain mapping objects with code, service type,
    display name, current boolean value, and command generation. Do not import Homebridge.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/mappers/scaling.test.ts src/mappers/switchOutlet.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

</tasks>

<verification>
- `rg -n "homebridge|hap-nodejs" src/mappers` has no matches.
- `npm test -- --runTestsByPath src/mappers/scaling.test.ts src/mappers/switchOutlet.test.ts --runInBand`
- `make check`
</verification>

