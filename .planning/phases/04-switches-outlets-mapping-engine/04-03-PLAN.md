---
phase: 04-switches-outlets-mapping-engine
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/accessories/switchOutletAccessory.ts
  - src/accessories/switchOutletAccessory.test.ts
  - src/platform/accessoryRegistry.ts
  - src/platform/accessoryRegistry.test.ts
  - src/platform.ts
autonomous: true
requirements: [SW-01, SW-02, SW-03]
must_haves:
  truths:
    - "Supported switch/outlet accessories bind HomeKit services during discovery reconciliation."
    - "A multi-gang device gets one service per switch DP using the DP code as subtype."
    - "onGet returns cached Tuya status and does not call the cloud."
    - "onSet sends a Tuya command and updates cached status only after success."
    - "platform.ts remains thin composition glue."
  artifacts:
    - path: "src/accessories/switchOutletAccessory.ts"
      provides: "HomeKit service binding for switch/outlet mappings"
    - path: "src/platform/accessoryRegistry.ts"
      provides: "Registry hook for accessory binding"
decisions_covered: [SW-01, SW-02, SW-03]
---

<objective>
Bind switch/outlet mappings to HomeKit services so discovered Tuya switches and outlets are useful
from the Home app.
</objective>

<context>
@.planning/phases/04-switches-outlets-mapping-engine/04-CONTEXT.md
@src/mappers/switchOutlet.ts
@src/discovery/deviceRepository.ts
@src/platform/accessoryRegistry.ts
@src/platform.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing switch/outlet accessory binder tests</name>
  <action>
    Create `src/accessories/switchOutletAccessory.test.ts` with lightweight fake HomeKit service and
    characteristic objects. Cover service creation/reuse, Switch vs Outlet selection, multi-gang
    subtypes, cached `onGet`, successful `onSet`, and failed `onSet` not mutating cached status.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/accessories/switchOutletAccessory.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 2: Implement switch/outlet accessory binder</name>
  <action>
    Add `src/accessories/switchOutletAccessory.ts`. It should accept a narrow HAP API surface,
    a registry accessory, a Tuya device, and a command sender. It should bind services from
    `buildSwitchOutletMappings`.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/accessories/switchOutletAccessory.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 3: Write failing registry integration tests</name>
  <action>
    Extend `src/platform/accessoryRegistry.test.ts` so reconciliation calls the binder for new and
    restored supported accessories, passes the current device metadata, and does not bind unsupported
    categories.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/platform/accessoryRegistry.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 4: Wire binder through registry and platform composition</name>
  <action>
    Add an optional bind callback to `AccessoryRegistry` options and pass it from `platform.ts`.
    The callback should send commands through `DeviceRepository.sendCommands`.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/accessories/switchOutletAccessory.test.ts src/platform/accessoryRegistry.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

</tasks>

<verification>
- `npm test -- --runInBand`
- `npx tsc --noEmit`
- `npm run lint`
- `make check`
</verification>

