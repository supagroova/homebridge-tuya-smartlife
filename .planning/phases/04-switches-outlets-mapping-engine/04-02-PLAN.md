---
phase: 04-switches-outlets-mapping-engine
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/discovery/deviceRepository.ts
  - src/discovery/deviceRepository.test.ts
autonomous: true
requirements: [SW-01]
must_haves:
  truths:
    - "DeviceRepository can send Tuya device commands through the signed client."
    - "Command payload uses POST /v1.1/m/thing/{deviceId}/commands."
    - "Repository does not swallow command failures."
  artifacts:
    - path: "src/discovery/deviceRepository.ts"
      provides: "sendCommands(deviceId, commands)"
decisions_covered: [SW-01]
---

<objective>
Add the Tuya command transport needed by HomeKit writes without introducing Homebridge coupling into
the discovery repository.
</objective>

<context>
@.planning/phases/04-switches-outlets-mapping-engine/04-CONTEXT.md
@src/discovery/deviceRepository.ts
@src/discovery/deviceRepository.test.ts
@src/auth/customerApi.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing command transport tests</name>
  <action>
    Extend `src/discovery/deviceRepository.test.ts` to cover `sendCommands(deviceId, commands)`.
    Assert the signed client receives `post('/v1.1/m/thing/{deviceId}/commands', undefined,
    { commands })` and that rejected client errors propagate.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/discovery/deviceRepository.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 2: Implement repository command method</name>
  <action>
    Extend the repository's narrow client interface with `post` and add `sendCommands`.
    Keep command type exported from discovery or mapper code so accessory binders can share it.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/discovery/deviceRepository.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

</tasks>

<verification>
- `npm test -- --runTestsByPath src/discovery/deviceRepository.test.ts --runInBand`
- `make check`
</verification>

