---
phase: 03-device-discovery-platform-skeleton
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/platformDiscovery.ts
  - src/platformDiscovery.test.ts
  - src/platform.ts
  - config.schema.json
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04]
must_haves:
  truths:
    - "didFinishLaunching triggers token load, discovery, and accessory reconciliation"
    - "Missing or expired auth is reported as re-auth-required and does not prune accessories"
    - "Whitelist config remains deferred; no whitelist fields are added"
  artifacts:
    - path: "src/platformDiscovery.ts"
      provides: "Testable platform discovery orchestration"
    - path: "src/platformDiscovery.test.ts"
      provides: "No-token, success, and discovery-error orchestration coverage"
    - path: "src/platform.ts"
      provides: "Homebridge composition hook into Phase 3 discovery"
  decisions_covered: [D-04, D-05, D-09, D-10, D-11]
---

<objective>
Wire discovery into the Homebridge platform startup while keeping the orchestration testable and the
existing platform adapter thin.
</objective>

<context>
@.planning/phases/03-device-discovery-platform-skeleton/03-CONTEXT.md
@src/platform.ts
@src/auth/tokenStore.ts
@src/auth/customerApi.ts
@src/discovery/deviceRepository.ts
@src/platform/accessoryRegistry.ts
</context>

<phase_artifacts>
- `src/platformDiscovery.ts`
- `src/platformDiscovery.test.ts`
- `src/platform.ts`
- `config.schema.json` only if needed for existing minimal config validity; do not add whitelist fields
</phase_artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Write failing platform discovery orchestration tests</name>
  <action>
    Create `src/platformDiscovery.test.ts` first. Cover no token -> re-auth-required/no discovery,
    token present -> repository discovery and registry reconcile called, discovery error -> logged and
    no prune/register side effects, and no whitelist behavior/config expected.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/platformDiscovery.test.ts --runInBand</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement testable discovery orchestration</name>
  <action>
    Add `src/platformDiscovery.ts` with a small orchestration function/class that accepts injected
    token store, client factory, repository factory, accessory registry, and logger. Keep token
    loading/auth error behavior explicit. Do not add whitelist config fields.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/platformDiscovery.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Connect orchestration to TuyaSmartLifePlatform</name>
  <action>
    Update `src/platform.ts` so `didFinishLaunching` calls the Phase 3 orchestration. Use Homebridge
    storage/local JSON semantics for the token file path; keep config requirements minimal until
    Phase 7. Preserve `configureAccessory` cache behavior. Do not add switch/thermometer services.
  </action>
  <verify>
    <automated>npm test -- --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/platform.ts` remains thin Homebridge glue.
    - `rg -n "homeIds|deviceIds|whitelist" src config.schema.json` has no Phase 3 config-field matches.
    - `make check` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Risks

- Running prune after auth failure or network error could delete valid cached accessories.
- Platform logs could leak token values.
- Pulling config UI/whitelist forward would expand scope.

## Mitigations

- Orchestration tests assert no registry reconcile happens when token/discovery fails.
- Use existing redacted auth errors and log generic re-auth messages.
- Explicit scan rejects whitelist fields in Phase 3.
</threat_model>

<verification>
- `npm test -- --runInBand`
- `rg -n "homeIds|deviceIds|whitelist" src config.schema.json || true` (review any matches; no config fields should exist)
- `make check`
</verification>

