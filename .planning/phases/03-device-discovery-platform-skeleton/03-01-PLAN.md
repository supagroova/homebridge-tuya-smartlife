---
phase: 03-device-discovery-platform-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/discovery/types.ts
  - src/discovery/deviceRepository.ts
  - src/discovery/deviceRepository.test.ts
autonomous: true
requirements: [DISC-01]
must_haves:
  truths:
    - "Discovery fetches homes then devices for each home"
    - "All discovered devices are represented in the discovery result, including unsupported categories"
    - "Device metadata preserves category, status map, function specs, status ranges, online state, and home id"
  artifacts:
    - path: "src/discovery/types.ts"
      provides: "Discovery model types for homes/devices/spec metadata"
    - path: "src/discovery/deviceRepository.ts"
      provides: "Tuya cloud discovery repository using TuyaDeviceSharingClient"
    - path: "src/discovery/deviceRepository.test.ts"
      provides: "Offline tests for homes/devices/spec normalization and errors"
  decisions_covered: [D-01, D-03, D-09, D-10, D-11]
---

<objective>
Implement the testable Tuya discovery repository: query homes, query devices for each home, enrich
device metadata, normalize Tuya response shapes, and return a complete discovery result without any
Homebridge registration side effects.
</objective>

<context>
@.planning/phases/03-device-discovery-platform-skeleton/03-CONTEXT.md
@.planning/phases/03-device-discovery-platform-skeleton/03-RESEARCH.md
@src/auth/customerApi.ts
@src/auth/errors.ts
</context>

<phase_artifacts>
- `src/discovery/types.ts`
- `src/discovery/deviceRepository.ts`
- `src/discovery/deviceRepository.test.ts`
</phase_artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Write failing discovery repository tests</name>
  <action>
    Create `src/discovery/deviceRepository.test.ts` before production discovery files. Use a narrow
    fake client with a `get(path, params)` method. Cover homes mapping (`ownerId` to string id),
    querying devices for each home, status-array-to-map normalization, specification functions/status
    ranges keyed by code, report type attachment, unsupported category inclusion, and auth/API error
    propagation.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/discovery/deviceRepository.test.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - The test command fails before `src/discovery/deviceRepository.ts` exists.
    - Fixtures include at least one switch-like device, one temperature/humidity device, and one unsupported category.
    - Tests do not use live network calls.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement discovery types and repository</name>
  <action>
    Add `src/discovery/types.ts` and `src/discovery/deviceRepository.ts`. Implement `queryHomes`,
    `queryDevicesByHome`, and `discoverDevices`. Use the existing signed client; do not add a new HTTP
    library. Normalize raw Tuya status arrays into a status map and preserve specs/functions/status
    ranges for later mapping phases.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/discovery/deviceRepository.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - All targeted tests pass.
    - No Homebridge imports appear in `src/discovery/*`.
    - Errors from the signed client propagate; discovery does not convert auth failures into empty results.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Risks

- Treating auth failure as "no devices" could prune valid accessories.
- Dropping status/spec metadata would force rework in switch and thermometer phases.

## Mitigations

- Tests assert error propagation.
- Tests assert status/spec metadata is preserved in normalized models.
</threat_model>

<verification>
- `npm test -- --runTestsByPath src/discovery/deviceRepository.test.ts --runInBand`
- `make check`
</verification>

