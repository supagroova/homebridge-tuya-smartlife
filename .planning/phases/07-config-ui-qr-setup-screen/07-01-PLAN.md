---
phase: 07-config-ui-qr-setup-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.schema.json
  - config.schema.test.mjs
  - src/platform.ts
  - src/platform.test.ts
autonomous: true
requirements: [CFG-01]
must_haves:
  truths:
    - "Homebridge settings expose region/endpoint, optional whitelist, and debug options without hand-editing JSON."
    - "The schema enables custom UI with customUi: true while preserving the TuyaSmartLife platform alias."
    - "Platform startup logs a clear auth/setup message when no token exists, so test installs are observable in logs."
  artifacts:
    - path: "config.schema.json"
      provides: "Homebridge config schema and custom UI opt-in"
decisions_covered: [CFG-01]
---

<objective>
Make the installed plugin visibly configurable in Homebridge UI and prepare the settings contract for
the QR login screen.
</objective>

<context>
@config.schema.json
@src/platform.ts
@src/platformDiscovery.ts
@.planning/ROADMAP.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing config schema tests</name>
  <action>
    Add a Node test for `config.schema.json` covering `customUi: true`, `pluginAlias:
    "TuyaSmartLife"`, `pluginType: "platform"`, `singular: true`, and schema properties for
    `name`, `endpoint`, `homeIds`, `deviceIds`, `debug`, and `pollIntervalSeconds`.
  </action>
  <verify>
    <automated>node --test config.schema.test.mjs</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 2: Expand schema and startup observability</name>
  <action>
    Update `config.schema.json` with the tested fields and custom UI flag. Add or extend a focused
    platform/startup test so missing-token startup logs a clear setup message, then implement the
    minimal platform logging change if needed.
  </action>
  <verify>
    <automated>node --test config.schema.test.mjs && npm test -- --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

</tasks>

<verification>
- `node --test config.schema.test.mjs`
- `npm test -- --runInBand`
- `npx tsc --noEmit`
- `npm run lint`
</verification>

