---
phase: 07-config-ui-qr-setup-screen
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - README.md
  - scripts/release-check.mjs
  - scripts/release-check.test.mjs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [PUB-02]
must_haves:
  truths:
    - "README documents install, QR setup, supported devices, known limitations, and troubleshooting."
    - "Release checks prove the package/tarball includes config.schema.json, dist/index.js, and homebridge-ui assets."
    - "Phase 7 completion updates GSD state and requirement traceability."
  artifacts:
    - path: "README.md"
      provides: "User-facing setup and troubleshooting guide"
decisions_covered: [PUB-02]
---

<objective>
Document the testable user workflow and close Phase 7 readiness without publishing prematurely.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@package.json
@config.schema.json
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend release checks for UI assets and docs</name>
  <action>
    Add release-check tests that fail if `README.md`, `homebridge-ui/public/index.html`, or
    `homebridge-ui/server.js` are absent from the package/git install file set.
  </action>
  <verify>
    <automated>node --test scripts/release-check.test.mjs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Write README setup guide</name>
  <action>
    Create `README.md` documenting install from npm/tarball during testing, opening the Homebridge
    plugin settings, entering Smart Life user code, scanning QR, restart expectations, supported
    switch/outlet/thermostat/sensor categories, and known limitations including HA-compatible
    credentials and polling-only updates.
  </action>
  <verify>
    <automated>npm run release:check</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 7 gate and update GSD state</name>
  <action>
    Run `make check`, `node --test homebridge-ui/server.test.mjs`, `npm run release:check`, and
    `npm pack --dry-run --json`. Update requirements/roadmap/state to mark CFG-01, CFG-02, and
    PUB-02 complete when the checks pass.
  </action>
  <verify>
    <automated>make check && node --test homebridge-ui/server.test.mjs && npm run release:check && npm pack --dry-run --json</automated>
  </verify>
</task>

</tasks>

<verification>
- `make check`
- `node --test homebridge-ui/server.test.mjs`
- `npm run release:check`
- `npm pack --dry-run --json`
</verification>

