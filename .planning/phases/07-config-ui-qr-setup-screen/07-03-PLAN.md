---
phase: 07-config-ui-qr-setup-screen
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - README.md
  - scripts/release-check.mjs
  - scripts/release-check.test.mjs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [PUB-02]
must_haves:
  truths:
    - "README documents install, QR setup, supported devices, known limitations, and troubleshooting."
    - "Release checks prove the package/tarball includes config.schema.json, dist/index.js, and homebridge-ui assets."
    - "README separates current PR/tarball testing install from eventual npm install."
    - "README documents that git+https installs are not recommended for Pi testing because npm runs Git dependency preparation."
    - "Phase 7 completion updates GSD state and requirement traceability."
  artifacts:
    - path: "README.md"
      provides: "User-facing setup and troubleshooting guide"
decisions_covered: [PUB-02]
context_decisions_covered: [D-10, D-11, D-12, D-13]
---

<objective>
Document the testable user workflow and close Phase 7 readiness without publishing prematurely.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@package.json
@config.schema.json
@.planning/phases/07-config-ui-qr-setup-screen/07-CONTEXT.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend release checks for UI assets and docs</name>
  <action>
    Add release-check tests that fail if `README.md`, `homebridge-ui/public/index.html`, or
    `homebridge-ui/server.js` are absent from the package/git install file set.
  </action>
  <acceptance_criteria>
    - `node --test scripts/release-check.test.mjs` fails before release checks know about README/UI assets.
    - Tests cover both `npm pack --dry-run` file contents and tracked git install files where applicable.
    - Tests preserve the no-`prepare` rule for low-memory Homebridge hosts.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/release-check.test.mjs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Write README setup guide</name>
  <action>
    Create `README.md` documenting install from npm/tarball during testing, opening the Homebridge
    plugin settings, entering Smart Life user code, scanning QR, restart expectations, supported
    switch/outlet/thermostat/sensor categories, and known limitations including HA-compatible
    credentials and polling-only updates.
  </action>
  <acceptance_criteria>
    - README documents current tarball install from a commit SHA for remote Homebridge testing.
    - README documents eventual npm install separately.
    - README explains QR setup through Homebridge plugin settings.
    - README includes troubleshooting for `git+https` SIGKILL / Git dependency preparation failures.
    - README lists supported v1 device categories and known limitations.
  </acceptance_criteria>
  <verify>
    <automated>npm run release:check</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 7 gate and update GSD state</name>
  <action>
    Run `make check`, `node --test homebridge-ui/server.test.mjs`, `npm run release:check`, and
    `npm pack --dry-run --json`. Update requirements/roadmap/state to mark CFG-01, CFG-02, and
    PUB-02 complete when the checks pass.
  </action>
  <acceptance_criteria>
    - `make check` passes.
    - `node --test homebridge-ui/server.test.mjs` passes.
    - `npm run release:check` passes and confirms `homebridge-ui` assets are included.
    - `npm pack --dry-run --json` passes.
    - `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` reflect Phase 7 completion only after verification.
  </acceptance_criteria>
  <verify>
    <automated>make check && node --test homebridge-ui/server.test.mjs && npm run release:check && npm pack --dry-run --json</automated>
  </verify>
</task>

</tasks>

<verification>
- `make check`
- `node --test homebridge-ui/server.test.mjs`
- `npm run release:check`
- `npm pack --dry-run --json`
</verification>
