---
phase: 06-status-polling-offline-handling-mvp-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/updates/updateHub.ts
  - src/updates/updateHub.test.ts
  - src/updates/poller.ts
  - src/updates/poller.test.ts
autonomous: true
requirements: [UPD-01, UPD-02]
must_haves:
  truths:
    - "UpdateHub stores the latest TuyaDevice snapshot by device id."
    - "UpdateHub emits subscriber events only when online status or status DP values change."
    - "Polling runs through DeviceRepository.discoverDevices and never calls HomeKit getters."
    - "Polling schedules delays with deterministic jitter and backs off after failures."
    - "Failed polls preserve the last successful device snapshot."
  artifacts:
    - path: "src/updates/updateHub.ts"
      provides: "Shared status snapshot cache and per-device subscription API"
    - path: "src/updates/poller.ts"
      provides: "Repository-backed polling scheduler with jitter/backoff"
decisions_covered: [UPD-01, UPD-02]
---

<objective>
Create the polling-first status update core that keeps Tuya device snapshots fresh without coupling
HomeKit getters to network calls.
</objective>

<context>
@.planning/phases/06-status-polling-offline-handling-mvp-ship/06-RESEARCH.md
@.planning/research/FEATURES.md
@src/discovery/deviceRepository.ts
@src/discovery/types.ts
@src/platformDiscovery.ts
</context>

<phase_artifacts>
- `src/updates/updateHub.ts`
- `src/updates/updateHub.test.ts`
- `src/updates/poller.ts`
- `src/updates/poller.test.ts`
</phase_artifacts>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing UpdateHub tests</name>
  <read_first>
    - `src/discovery/types.ts`
    - `src/platform/accessoryRegistry.ts`
    - `.planning/phases/06-status-polling-offline-handling-mvp-ship/06-RESEARCH.md`
  </read_first>
  <action>
    Create `src/updates/updateHub.test.ts`. Cover `replaceAll(devices)`, `applySnapshot(device)`,
    `get(deviceId)`, `subscribe(deviceId, listener)`, unsubscribe behavior, no event for identical
    snapshots, event for changed DP values, and event for `online` true/false changes.
  </action>
  <acceptance_criteria>
    - `npm test -- --runTestsByPath src/updates/updateHub.test.ts --runInBand` fails because `src/updates/updateHub.ts` does not exist.
    - The test file asserts that identical snapshots do not notify listeners.
    - The test file asserts that an offline transition notifies listeners with the updated device.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runTestsByPath src/updates/updateHub.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 2: Implement UpdateHub</name>
  <read_first>
    - `src/updates/updateHub.test.ts`
    - `src/discovery/types.ts`
  </read_first>
  <action>
    Add `src/updates/updateHub.ts` exporting `UpdateHub`, `DeviceUpdateListener`, and
    `DeviceUpdateEvent`. Implement a `Map<string, TuyaDevice>` cache keyed by `device.id`, an
    `applySnapshot(device)` method that compares `device.online` and shallow `device.status` values,
    a `replaceAll(devices)` method that applies each snapshot, a `get(deviceId)` method, and a
    `subscribe(deviceId, listener)` method returning an unsubscribe function.
  </action>
  <acceptance_criteria>
    - `src/updates/updateHub.ts` exports `UpdateHub`.
    - `src/updates/updateHub.ts` imports only local project types and has no Homebridge imports.
    - `npm test -- --runTestsByPath src/updates/updateHub.test.ts --runInBand` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runTestsByPath src/updates/updateHub.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 3: Write failing poller tests</name>
  <read_first>
    - `src/discovery/deviceRepository.ts`
    - `src/platformDiscovery.ts`
    - `src/updates/updateHub.ts`
  </read_first>
  <action>
    Create `src/updates/poller.test.ts` with Jest fake timers. Cover immediate first poll,
    successful poll calling `updateHub.replaceAll(result.devices)`, jittered scheduling from
    `intervalMs` and `jitterRatio`, failure logging without clearing cached snapshots, exponential
    backoff up to `maxBackoffMs`, and `stop()` preventing later polls.
  </action>
  <acceptance_criteria>
    - `npm test -- --runTestsByPath src/updates/poller.test.ts --runInBand` fails because `src/updates/poller.ts` does not exist.
    - The tests inject deterministic random values instead of relying on `Math.random`.
    - The tests assert a failed poll does not call `updateHub.replaceAll([])`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runTestsByPath src/updates/poller.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 4: Implement repository-backed poller</name>
  <read_first>
    - `src/updates/poller.test.ts`
    - `src/updates/updateHub.ts`
    - `src/discovery/deviceRepository.ts`
    - `src/platformDiscovery.ts`
  </read_first>
  <action>
    Add `src/updates/poller.ts` exporting `DeviceStatusPoller` and `DeviceStatusPollerOptions`.
    The options should include `repository`, `updateHub`, `log`, `intervalMs`, `jitterRatio`,
    `backoffMultiplier`, `maxBackoffMs`, and optional `random`. Implement `start()` as an immediate
    poll followed by scheduled polls, `stop()` as timer cleanup, success as `updateHub.replaceAll`,
    and failure as warning log plus backoff scheduling while preserving the existing hub cache.
  </action>
  <acceptance_criteria>
    - `src/updates/poller.ts` exports `DeviceStatusPoller`.
    - `npm test -- --runTestsByPath src/updates/updateHub.test.ts src/updates/poller.test.ts --runInBand` passes.
    - `npx tsc --noEmit` passes.
    - `npm run lint` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runTestsByPath src/updates/updateHub.test.ts src/updates/poller.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

</tasks>

<verification>
- `npm test -- --runTestsByPath src/updates/updateHub.test.ts src/updates/poller.test.ts --runInBand`
- `rg -n "homebridge|hap-nodejs" src/updates || true` (review any matches; update core should stay Homebridge-free)
- `npx tsc --noEmit`
- `npm run lint`
</verification>

## Artifacts This Phase Produces

- `src/updates/updateHub.ts`
  - `UpdateHub`
  - `DeviceUpdateEvent`
  - `DeviceUpdateListener`
- `src/updates/poller.ts`
  - `DeviceStatusPoller`
  - `DeviceStatusPollerOptions`
