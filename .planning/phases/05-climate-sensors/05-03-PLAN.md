---
phase: 05-climate-sensors
plan: 03
type: execute
wave: 3
depends_on: [05-01]
files_modified:
  - src/mappers/thermostat.ts
  - src/mappers/thermostat.test.ts
autonomous: true
requirements: [CLIM-03, CLIM-04]
must_haves:
  truths:
    - "Thermostat mapping is pure and has no Homebridge imports."
    - "wk current and target temperatures are scaled from Tuya integer specs."
    - "Target temperature commands clamp to the temp_set range and round through the Tuya scale."
    - "Mode/switch mapping supports off plus a conservative heat/auto path."
    - "Battery mapping is available for thermostat devices."
  artifacts:
    - path: "src/mappers/thermostat.ts"
      provides: "Pure wk thermostat mapper and command generator"
decisions_covered: [CLIM-03, CLIM-04]
---

<objective>
Create pure `wk` thermostat mapping and command generation before binding it to HomeKit.
</objective>

<context>
@.planning/phases/05-climate-sensors/05-CONTEXT.md
@.planning/phases/05-climate-sensors/05-RESEARCH.md
@src/mappers/scaling.ts
@src/mappers/sensor.ts
@src/discovery/types.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing thermostat mapper tests</name>
  <action>
    Create `src/mappers/thermostat.test.ts`. Cover current/target temperature scaling, target
    min/max/step from `temp_set`, target temperature command clamping, switch off mapping,
    conservative target mode command generation, battery reuse, and unsupported categories.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/mappers/thermostat.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 2: Implement thermostat mapper</name>
  <action>
    Add `src/mappers/thermostat.ts` for category `wk` only. Reuse scaling and battery helpers.
    Keep HomeKit constants out of this file; return plain semantic descriptors and command builders.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/mappers/thermostat.test.ts src/mappers/scaling.test.ts src/mappers/sensor.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

</tasks>

<verification>
- `rg -n "homebridge|hap-nodejs" src/mappers/thermostat.ts` has no matches.
- `npm test -- --runTestsByPath src/mappers/thermostat.test.ts --runInBand`
- `make check`
</verification>

