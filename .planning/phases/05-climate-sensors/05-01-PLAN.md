---
phase: 05-climate-sensors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mappers/sensor.ts
  - src/mappers/sensor.test.ts
autonomous: true
requirements: [CLIM-01, CLIM-02, CLIM-04]
must_haves:
  truths:
    - "Sensor mapping is pure and has no Homebridge imports."
    - "wsdcg temperature and humidity values are scaled from Tuya integer specs."
    - "mcs, pir, sj, and ywbj binary sensor on-values are category-specific."
    - "Battery mapping is reusable and optional."
  artifacts:
    - path: "src/mappers/sensor.ts"
      provides: "Pure read-only sensor and battery mapper"
decisions_covered: [CLIM-01, CLIM-02, CLIM-04]
---

<objective>
Create pure mapper support for read-only temperature/humidity, binary sensors, and battery metadata.
</objective>

<context>
@.planning/phases/05-climate-sensors/05-CONTEXT.md
@.planning/phases/05-climate-sensors/05-RESEARCH.md
@src/mappers/scaling.ts
@src/discovery/types.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing read-only sensor mapper tests</name>
  <action>
    Create `src/mappers/sensor.test.ts`. Cover `wsdcg` temperature/humidity scaling, missing
    scale specs, `mcs` contact, `pir` motion, `sj` leak, `ywbj` smoke, battery percentage, low
    battery, and unsupported categories returning no mappings.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/mappers/sensor.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 2: Implement read-only sensor mapper</name>
  <action>
    Add `src/mappers/sensor.ts`. Return plain descriptor objects for HomeKit-facing binders.
    Reuse `parseIntegerSpec` and `scaleTuyaInteger`; do not import Homebridge.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/mappers/sensor.test.ts src/mappers/scaling.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

</tasks>

<verification>
- `rg -n "homebridge|hap-nodejs" src/mappers/sensor.ts` has no matches.
- `npm test -- --runTestsByPath src/mappers/sensor.test.ts --runInBand`
- `make check`
</verification>

