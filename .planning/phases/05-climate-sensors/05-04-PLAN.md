---
phase: 05-climate-sensors
plan: 04
type: execute
wave: 4
depends_on: [05-02, 05-03]
files_modified:
  - src/accessories/thermostatAccessory.ts
  - src/accessories/thermostatAccessory.test.ts
  - src/platform/accessoryRegistry.test.ts
  - src/platform.ts
  - src/discovery/supportedCategories.ts
  - src/discovery/supportedCategories.test.ts
autonomous: true
requirements: [CLIM-01, CLIM-02, CLIM-03, CLIM-04]
must_haves:
  truths:
    - "wk category is supported for registration."
    - "Thermostat binder creates/reuses a HomeKit Thermostat service."
    - "Thermostat onGet handlers are cached-only."
    - "Target temperature and target mode setters send Tuya commands and update cached status only after success."
    - "Platform bind callback invokes switch/outlet, sensor, and thermostat binders in order."
  artifacts:
    - path: "src/accessories/thermostatAccessory.ts"
      provides: "HomeKit Thermostat binding for wk devices"
    - path: "src/platform.ts"
      provides: "Phase 5 binder composition"
decisions_covered: [CLIM-01, CLIM-02, CLIM-03, CLIM-04]
---

<objective>
Bind thermostat support into HomeKit and wire Phase 5 binders into platform discovery.
</objective>

<context>
@.planning/phases/05-climate-sensors/05-CONTEXT.md
@src/mappers/thermostat.ts
@src/accessories/sensorAccessory.ts
@src/accessories/switchOutletAccessory.ts
@src/platform/accessoryRegistry.ts
@src/platform.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing wk supported-category tests</name>
  <action>
    Extend `src/discovery/supportedCategories.test.ts` to include thermostat category `wk`.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/discovery/supportedCategories.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 2: Add wk supported category</name>
  <action>
    Update `src/discovery/supportedCategories.ts` with `wk`.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/discovery/supportedCategories.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 3: Write failing thermostat accessory binder tests</name>
  <action>
    Create `src/accessories/thermostatAccessory.test.ts` with fake HAP services/characteristics.
    Cover service creation/reuse, cached current/target temperature getters, target temperature
    setter, target mode setter/off command, battery characteristics, and failed setters not mutating
    cached status.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/accessories/thermostatAccessory.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 4: Implement thermostat binder and platform composition</name>
  <action>
    Add `src/accessories/thermostatAccessory.ts` and update `src/platform.ts` so the bind callback
    invokes switch/outlet, sensor, and thermostat binders. Extend registry tests only if composition
    behavior needs a new assertion.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/accessories/thermostatAccessory.test.ts src/platform/accessoryRegistry.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

</tasks>

<verification>
- `npm test -- --runInBand`
- `npx tsc --noEmit`
- `npm run lint`
- `make check`
</verification>

