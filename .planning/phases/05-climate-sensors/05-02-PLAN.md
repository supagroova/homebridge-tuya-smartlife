---
phase: 05-climate-sensors
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/accessories/sensorAccessory.ts
  - src/accessories/sensorAccessory.test.ts
  - src/discovery/supportedCategories.ts
  - src/discovery/supportedCategories.test.ts
autonomous: true
requirements: [CLIM-01, CLIM-02, CLIM-04]
must_haves:
  truths:
    - "wsdcg, mcs, pir, sj, and ywbj categories are supported for registration."
    - "Sensor binder creates HomeKit TemperatureSensor, HumiditySensor, ContactSensor, MotionSensor, LeakSensor, SmokeSensor, and battery characteristics as applicable."
    - "Sensor onGet handlers are cached-only and perform no cloud I/O."
  artifacts:
    - path: "src/accessories/sensorAccessory.ts"
      provides: "HomeKit service binding for read-only sensors"
decisions_covered: [CLIM-01, CLIM-02, CLIM-04]
---

<objective>
Bind read-only sensor mapper descriptors to HomeKit services and extend the supported category set.
</objective>

<context>
@.planning/phases/05-climate-sensors/05-CONTEXT.md
@src/mappers/sensor.ts
@src/accessories/switchOutletAccessory.ts
@src/discovery/supportedCategories.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing supported-category tests</name>
  <action>
    Extend `src/discovery/supportedCategories.test.ts` to cover `mcs`, `pir`, `sj`, and `ywbj`
    in addition to existing `wsdcg`.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/discovery/supportedCategories.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 2: Extend supported categories</name>
  <action>
    Update `src/discovery/supportedCategories.ts` with the Phase 5 read-only sensor categories.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/discovery/supportedCategories.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 3: Write failing sensor accessory binder tests</name>
  <action>
    Create `src/accessories/sensorAccessory.test.ts` using fake HAP services/characteristics.
    Cover temperature/humidity services, binary sensor services, battery characteristics, service
    reuse by subtype, and cached-only `onGet`.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/accessories/sensorAccessory.test.ts --runInBand</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 4: Implement sensor accessory binder</name>
  <action>
    Add `src/accessories/sensorAccessory.ts`. It should consume `buildSensorMappings`, create or
    reuse HomeKit services, and wire cached `onGet` handlers.
  </action>
  <verify>
    <automated>npm test -- --runTestsByPath src/accessories/sensorAccessory.test.ts src/discovery/supportedCategories.test.ts --runInBand && npx tsc --noEmit && npm run lint</automated>
  </verify>
</task>

</tasks>

<verification>
- `npm test -- --runTestsByPath src/mappers/sensor.test.ts src/accessories/sensorAccessory.test.ts src/discovery/supportedCategories.test.ts --runInBand`
- `make check`
</verification>

