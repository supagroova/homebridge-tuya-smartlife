---
phase: 08-v1-release-hardening-npm-publish
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [REL-08]
must_haves:
  truths:
    - "The remote Homebridge server installs the package from npm, not a GitHub tarball."
    - "Homebridge discovers the plugin from the npm install."
    - "QR login still completes from the Homebridge custom UI."
    - "The user's switches and thermometers appear in HomeKit after restart/discovery."
    - "Known release limitations remain documented."
  artifacts:
    - path: ".planning/phases/08-v1-release-hardening-npm-publish/08-04-SUMMARY.md"
      provides: "Post-publish smoke-test evidence and release closeout"
decisions_covered: [REL-08]
---

<objective>
Verify the public npm package works in the real Homebridge environment and close the v1.0 release milestone.
</objective>

<context>
@.planning/phases/08-v1-release-hardening-npm-publish/08-CONTEXT.md
@README.md
@package.json
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="manual">
  <name>Task 1: Install from npm on the remote Homebridge host</name>
  <action>
    Have the user install `homebridge-tuya-smartlife` from npm on the remote Homebridge server and restart Homebridge.
  </action>
  <acceptance_criteria>
    - Install command uses npm package name, not GitHub tarball or `git+https`.
    - Homebridge UI shows the plugin in the plugin list.
    - Homebridge logs do not show startup errors from the plugin.
  </acceptance_criteria>
  <verify>
    <manual>Remote Homebridge install and restart output supplied by user.</manual>
  </verify>
</task>

<task type="manual">
  <name>Task 2: Smoke-test QR login and devices</name>
  <action>
    Complete QR login from plugin settings, restart Homebridge if prompted, and verify switches and thermometers appear in HomeKit.
  </action>
  <acceptance_criteria>
    - QR code renders in Homebridge UI.
    - Smart Life scan/confirmation completes.
    - UI reports connected state.
    - Homebridge restart/discovery completes.
    - User confirms switches and thermometers appear in the Home app.
    - Default logs do not include sensitive values or debug-only diagnostics.
  </acceptance_criteria>
  <verify>
    <manual>User confirms QR login and HomeKit device visibility.</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Close Phase 8 and release milestone</name>
  <action>
    Update README only if smoke testing reveals install/setup corrections. Then update requirements,
    roadmap, and state to mark `REL-08` and Phase 8 complete.
  </action>
  <acceptance_criteria>
    - `REL-08` is complete only after real Homebridge npm-package smoke testing passes.
    - Phase 8 roadmap status is complete.
    - State points to release completion / next milestone.
    - README reflects the npm install path and any smoke-test corrections.
  </acceptance_criteria>
  <verify>
    <automated>make check && npm run release:check && git diff --check</automated>
  </verify>
</task>

</tasks>

<verification>
- Remote Homebridge npm install
- QR login via Homebridge custom UI
- HomeKit device visibility confirmation
- `make check`
- `npm run release:check`
</verification>
