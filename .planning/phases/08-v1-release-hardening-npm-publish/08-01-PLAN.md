---
phase: 08-v1-release-hardening-npm-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/auth/qrLoginFlow.ts
  - src/auth/qrLoginFlow.test.ts
  - src/auth/errors.ts
  - src/auth/customerApi.test.ts
  - homebridge-ui/server.js
  - homebridge-ui/server.test.mjs
autonomous: true
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "Verbose QR/auth diagnostics are emitted only when the existing plugin debug config is enabled."
    - "Default logs do not include Tuya QR request/response diagnostics."
    - "Tests prove user codes, QR tokens, access tokens, refresh tokens, token JSON, encrypted payloads, encrypted request data, and request signatures are not logged."
    - "Useful non-sensitive operational logs remain available for setup failures and re-auth guidance."
  artifacts:
    - path: "src/auth/qrLoginFlow.test.ts"
      provides: "Regression coverage for debug gating and QR-token redaction"
    - path: "homebridge-ui/server.test.mjs"
      provides: "UI server coverage for debug-gated QR setup diagnostics"
decisions_covered: [REL-01, REL-02]
---

<objective>
Make release logging safe by default while keeping opt-in diagnostics available through the existing `debug` config flag.
</objective>

<context>
@.planning/phases/08-v1-release-hardening-npm-publish/08-CONTEXT.md
@config.schema.json
@src/platform.ts
@src/auth/qrLoginFlow.ts
@src/auth/errors.ts
@homebridge-ui/server.js
@homebridge-ui/server.test.mjs
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Reproduce default debug leakage</name>
  <action>
    Add tests showing that QR start/poll diagnostics and Tuya QR request/response diagnostics are not emitted unless debug is enabled.
  </action>
  <acceptance_criteria>
    - A failing test covers the current default path where QR diagnostics are logged despite debug being unset/false.
    - A passing path still proves diagnostics are emitted when debug is true.
    - Tests assert exact absence of "Tuya QR request", "Tuya QR response", "QR login start requested", and "QR login poll requested" in default logs.
  </acceptance_criteria>
  <verify>
    <automated>node --test homebridge-ui/server.test.mjs && npm test -- --runTestsByPath src/auth/qrLoginFlow.test.ts</automated>
  </verify>
</task>

<task type="tdd">
  <name>Task 2: Expand sensitive logging regression coverage</name>
  <action>
    Add focused tests for sensitive values that must not appear in logs, including user code, QR token,
    access token, refresh token, token JSON, `encdata`, `X-sign`, and `X-token`.
  </action>
  <acceptance_criteria>
    - Tests fail if any sensitive sample value appears in captured log calls.
    - Tests cover both UI-server logging and auth/client redaction helpers where applicable.
    - Existing token persistence tests remain unchanged; token files may contain credentials, logs may not.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runTestsByPath src/auth/qrLoginFlow.test.ts src/auth/customerApi.test.ts && node --test homebridge-ui/server.test.mjs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Implement debug-gated logging</name>
  <action>
    Thread the existing `debug` config value into the UI server/auth flow logging path and ensure diagnostic
    helpers no-op when debug is false. Keep warning/error logs for user-actionable setup failures.
  </action>
  <acceptance_criteria>
    - Default config does not emit QR/auth diagnostics.
    - Debug config true emits sanitized diagnostics useful for troubleshooting.
    - No sensitive value appears in logs in either mode.
    - The change does not require new user-facing config fields.
  </acceptance_criteria>
  <verify>
    <automated>make check && node --test homebridge-ui/server.test.mjs</automated>
  </verify>
</task>

</tasks>

<verification>
- `node --test homebridge-ui/server.test.mjs`
- `npm test -- --runTestsByPath src/auth/qrLoginFlow.test.ts src/auth/customerApi.test.ts`
- `make check`
</verification>
