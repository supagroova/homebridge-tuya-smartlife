---
phase: 08-v1-release-hardening-npm-publish
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - package.json
  - package-lock.json
  - README.md
  - CHANGELOG.md
  - scripts/release-check.mjs
  - scripts/release-check.test.mjs
autonomous: true
requirements: [REL-03, REL-04, REL-05, REL-06]
must_haves:
  truths:
    - "Package version is exactly 1.0.0."
    - "CHANGELOG.md documents the initial v1.0.0 release."
    - "README contains npm version and download badges."
    - "README does not show the Homebridge verified badge unless verification has actually been granted."
    - "Release checks cover version, changelog, README badges, package files, and Homebridge discovery metadata."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Initial v1.0.0 release notes"
    - path: "README.md"
      provides: "Release-facing install docs and badges"
decisions_covered: [REL-03, REL-04, REL-05, REL-06]
---

<objective>
Prepare package metadata and user-facing docs for the `1.0.0` npm release.
</objective>

<context>
@.planning/phases/08-v1-release-hardening-npm-publish/08-CONTEXT.md
@package.json
@package-lock.json
@README.md
@scripts/release-check.mjs
@scripts/release-check.test.mjs
@config.schema.json
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add release metadata checks first</name>
  <action>
    Extend `scripts/release-check.test.mjs` so it fails until version, changelog, README badges,
    pack contents, and Homebridge metadata satisfy the v1.0 release rules.
  </action>
  <acceptance_criteria>
    - Tests assert `package.json.version === "1.0.0"`.
    - Tests assert `CHANGELOG.md` exists and mentions `1.0.0`.
    - Tests assert README contains the npm version and npm download badges.
    - Tests explicitly reject adding the Homebridge verified badge before a documented verification marker exists.
    - Tests preserve Homebridge discovery checks for package name, keyword, schema alias/type, `dist`, and UI assets.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/release-check.test.mjs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update version, changelog, and README badges</name>
  <action>
    Set `package.json` and `package-lock.json` to version `1.0.0`, add `CHANGELOG.md`, and add the
    npm version/download badges at the top of `README.md`. Do not add the Homebridge verified badge
    unless verification has already been granted.
  </action>
  <acceptance_criteria>
    - `npm install --package-lock-only` updates the lockfile without changing dependencies.
    - `CHANGELOG.md` has an initial `1.0.0` section covering QR login, supported devices, polling, config UI, and known limitations.
    - README install docs are updated from "future npm install" to release install guidance.
    - The Homebridge verified badge remains absent or is clearly gated on actual approval.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/release-check.test.mjs && npm run release:check</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Run release documentation gate</name>
  <action>
    Run the release checks and package dry-run to verify docs and metadata are consistent with the package contents.
  </action>
  <acceptance_criteria>
    - `npm run release:check` passes.
    - `npm pack --dry-run --json` includes `README.md`, `CHANGELOG.md`, `config.schema.json`, `dist/index.js`, `homebridge-ui/public/index.html`, and `homebridge-ui/server.js`.
    - `make check` passes after version/docs changes.
  </acceptance_criteria>
  <verify>
    <automated>make check && npm run release:check && npm pack --dry-run --json</automated>
  </verify>
</task>

</tasks>

<verification>
- `node --test scripts/release-check.test.mjs`
- `make check`
- `npm run release:check`
- `npm pack --dry-run --json`
</verification>
